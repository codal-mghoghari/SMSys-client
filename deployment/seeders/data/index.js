const {seedCoursesData} = require('./coursesData')
const {seedQuestionsData, seedOptionsData} = require('./questionsData')

module.exports = {
    seedCoursesData,
    seedQuestionsData,
    seedOptionsData
}